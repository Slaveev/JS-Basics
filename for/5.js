function textCount ( input ) {
    let text = input[0]
    for ( let i = 0; i < text.length; i++ ) {
        let letters = text[i]
        console.log(letters)
    }
}
textCount(['Softaunimaina'])